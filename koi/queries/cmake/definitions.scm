; The first argument of `function(...)` / `macro(...)` is its name.

[
  (function_command (function) (argument_list . (argument) @definition))
  (macro_command (macro) (argument_list . (argument) @definition))
]
